import React from 'react';

// Correctly define the component type with React.FC (FunctionComponent)
const ApiEndpoints: React.FC = () => {
  return (
    <div>
      hello
    </div>
  );
};

export default ApiEndpoints;
